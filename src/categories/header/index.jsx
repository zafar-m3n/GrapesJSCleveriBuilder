import { addHeaderA } from "./HeaderA";
import { addHeaderB } from "./HeaderB";
import { addHeaderC } from "./HeaderC";

export const addAllHeaders = (editor) => {
  addHeaderA(editor);
  addHeaderB(editor);
  addHeaderC(editor);
};
